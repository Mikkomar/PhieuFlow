<div class="flex flex-col gap-[8px]">
    <span class="text-[13px] font-semibold text-text">
        @Label
        @if (Required)
        {
            <span class="text-danger"> *</span>
        }
    </span>

    @ChildContent

    @if (!string.IsNullOrWhiteSpace(Error))
    {
        <span class="text-[11.5px] text-danger" role="alert">@Error</span>
    }
    else if (!string.IsNullOrWhiteSpace(Helper))
    {
        <span class="text-[11.5px] text-text-faintest">@Helper</span>
    }
</div>

@code {
    [Parameter, EditorRequired]
    public string Label { get; set; } = null!;

    [Parameter]
    public bool Required { get; set; }

    [Parameter]
    public string? Helper { get; set; }

    /// <summary>A validation message shown in place of <see cref="Helper"/> when set.</summary>
    [Parameter]
    public string? Error { get; set; }

    [Parameter, EditorRequired]
    public RenderFragment ChildContent { get; set; } = null!;
}
